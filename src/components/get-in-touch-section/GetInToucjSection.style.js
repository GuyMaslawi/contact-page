import styled from "@emotion/styled";

export const GetInTouchSectionWrapper = styled("div")({
  padding: "80px",
  backgroundColor: "#f6f6f6",
});

export const Title = styled("h2")({
  margin: "0 0 20px",
  fontSize: "45px",
  textAlign: "center",
});
