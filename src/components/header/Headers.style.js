import styled from "@emotion/styled";

export const Wrapper = styled("div")({
  position: "fixed",
  top: 0,
  left: 0,
  right: 0,
  zIndex: 1,
  width: "100%",
  minHeight: "110px",
  padding: "24px",
  alignItems: "center",
  backgroundColor: "#fff",
});

export const Img = styled("img")({
  maxWidth: "100%",
  height: "auto",
});
